<ui:DialogWindow x:Class="Invert.uFrame.VS.Windows.SelectItemTypeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.12.0"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:windowViewModels="clr-namespace:Invert.GraphDesigner.WPF;assembly=Invert.GraphDesigner.WPF"
        mc:Ignorable="d"
        Title="SelectItemTypeWindow" Height="340" Width="204">
    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="36"/>
        </Grid.RowDefinitions>
        <ListBox ItemsSource="{Binding Items}" Margin="0,0,0,1" SelectedItem="{Binding Selected, Mode=TwoWay}" ItemTemplate="{DynamicResource ItemDisplay}" >
            <ListBox.Resources>
                <DataTemplate x:Key="ItemDisplay">
                    <Grid Height="32" Width="Auto">
                        <Label Content="{Binding Title}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"  />
                    </Grid>
                </DataTemplate>
            </ListBox.Resources>
        </ListBox>
        <Button Content="OK" Margin="111,0,10,0" Grid.Row="1" VerticalAlignment="Top"  Height="28" Command="{Binding OkCommand}" />

    </Grid>
</ui:DialogWindow>
